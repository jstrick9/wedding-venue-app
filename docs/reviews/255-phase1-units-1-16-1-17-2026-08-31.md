# Review #255 — Phase 1 Units 1.16–1.17: VenueChatPanel, OperationsSettingsManagement

**Date:** 2026-08-31 · **Mode:** continuous campaign · Batch note: 1.16 was a pure dead-state removal (zero behavior change); 1.17 is a bug unit with its own pinning test. One shared gate cycle.

## Unit 1.16 — `src/components/VenueChatPanel.tsx` (595 → 592 lines)

Original, properly typed code — zero tsc errors on de-blinding. Pruned: unused `getConfig` import; `showQuickReplies` state (written via `setShowQuickReplies(false)` on send, never read — the quick-reply bar renders unconditionally, so this was leftover state from a collapsible-bar design that never shipped; the feature itself works). **No defects.**

## Unit 1.17 — `admin/OperationsSettingsManagement.tsx` (436 → 435 lines)

**F-255-1 (P3 UX bug — fixed): every Operations-panel action has saved silently since creation.** The panel destructured `onShowSuccess` from `AdminCommonProps`, but the prop is named `showSuccess` (AdminTabTypes.ts:146; AdminPanel passes it via `commonProps` at line 1223). `onShowSuccess` was always `undefined`, so the 5 optional-chained calls never fired:

- add default checklist item → no "Default checklist item added!" toast
- remove checklist item → no toast
- add operational zone → no toast
- remove operational zone → no toast
- reset to defaults → no toast

Data still saved (the actions themselves work), so severity P3 not P1 — but users got zero feedback on mutating actions (double-add risk). All 5 sites now call `showSuccess(...)`. Not a crash because of the optional chaining — the same wrong-name pattern as F-252-1, defused by `?.`.

## Pinned by

`OperationsSettingsManagement.typing.test.ts` (1 test): all 5 sites call `showSuccess(`; the phantom `onShowSuccess` name is gone entirely; the destructure uses the real prop name.

## Gates (batch)

tsc + strict unused-locals scan clean (non-test) · eslint 0 errors / 30 warnings · vitest **1022 passed** / 5 skipped (+1) · single-file 546.39 kB gzip + split chunks within budget · audit clean · ratchet 7 → **5**.

## CI cycle note (first push red, second green)

The first push failed after the test step: `OperationsSettingsManagement.test.tsx`'s dummy props mocked the **phantom** name (`onShowSuccess: vi.fn()`) — the fixture had codified the F-255-1 bug, which is why the missing toasts never failed a test. With the fix, the "adds a new zone" click threw `showSuccess is not a function` as an unhandled error (surfaced in CI's timing; the local full-suite run had passed). Fixture upgraded to `showSuccess: vi.fn()` — per campaign rule, tighten fixtures, never loosen the type.

## Registry delta

Rows 1.16, 1.17 → `done`. Phase 1: **19/24**. Next: 1.18 `admin/SecurityAuditManagement.tsx` (345).
