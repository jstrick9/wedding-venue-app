# Review #257 — Phase 1 Units 1.20–1.22: CoupleLayoutEditor, VendorPanel, CoupleLayoutPreview — **PHASE 1 COMPLETE**

**Date:** 2026-08-31 · **Mode:** continuous campaign · Batch note: all three units were clean (one unused import between them); one shared gate cycle.

## Unit 1.20 — `src/components/CoupleLayoutEditor.tsx` (310 lines)

Zero tsc errors, zero strict-scan violations on de-blinding. **No defects, nothing to prune** — the cleanest unit of the campaign.

## Unit 1.21 — `src/components/VendorPanel.tsx` (296 lines)

Zero tsc errors. Strict scan: unused `VendorCategoryDef` type import pruned. **No defects.**

## Unit 1.22 — `src/components/CoupleLayoutPreview.tsx` (207 lines)

Zero tsc errors, zero strict-scan violations. **No defects.**

## Phase 1 closed

All 24 `@ts-nocheck` runtime files (~18.6 K lines at campaign start) are now fully type-checked. The ratchet ceiling is **0**: `scripts/check-ts-nocheck-ratchet.mjs` now fails CI if ANY runtime file under `src/` reintroduces `@ts-nocheck` (the ratchet test's "below ceiling" case was adapted to an explicit hypothetical ceiling since one file now legitimately fails).

Phase 1 totals: 24 files retyped · ~2,900 lines of dead/invalid code removed · defects found and fixed: F-250-1 (P1 FileReader crash), F-250-5 (P1 FileReader crash), F-251-1 (P2 type gap), F-251-2 (P1 venue-category toggle), F-251-4 (P1 same in TableManagement), F-251-5/7 (type-model lies), F-251-6 (P2 MouseEvent-as-tab), F-252-1 (P1 Spacing panel dead), F-253-1 (P1 calendar event form crash), F-253-2 (P4 endTime never shown), F-255-1 (P3 Operations toasts), F-256-1 (P3 Security+Templates toasts, 10 sites), F-256-2 (P3 fake save) — 13 real defects, 5 of them P1 "feature dead since creation."

## Pinned by

No new pinning tests this batch (no defects). Existing pins: `SpacingManagement.typing.test.ts`, `VenueCalendar.typing.test.ts`, `OperationsSettingsManagement.typing.test.ts`, `SecurityAuditManagement.typing.test.ts`, `CommunicationTemplatesManagement.typing.test.ts`; the ratchet itself (ceiling 0) pins the no-regression guarantee.

## Gates (batch)

tsc + strict unused-locals scan clean (non-test) · eslint 0 errors / 30 warnings · vitest **1026 passed** / 5 skipped · single-file 546.50 kB gzip + split chunks within budget · audit clean · ratchet 3 → **0**.

## Registry delta

Rows 1.20, 1.21, 1.22 → `done`. **Phase 1: 24/24 — COMPLETE.** Next: Phase 2 (RPC audit, 46 call sites).
