# Review #252 — Phase 1 Units 1.8–1.10: ChairManagement, WallManagement, SpacingManagement

**Date:** 2026-08-31 · **Mode:** continuous campaign · Batch note: 1.8/1.9 were pure dead-code removals (zero behavior change) and share one gate cycle with 1.10; each has its own registry row.

## Unit 1.8 — `admin/ChairManagement.tsx` (876 → 612 lines)

Clone-stamp garbage only: 57 invalid destructure names + 202 unused bindings removed; types import 12 → 2. **No defects.**

## Unit 1.9 — `admin/WallManagement.tsx` (803 → 540 lines)

Clone-stamp garbage only: 57 invalid + 201 unused names removed. **No defects.**

## Unit 1.10 — `admin/SpacingManagement.tsx` (792 → 546 lines)

**F-252-1 (P1 runtime bug — fixed): the entire Spacing panel has been non-functional since creation.** Every one of its 12 controls (collision-detection toggle, collision-warnings toggle, and all spacing inputs) called `setSpacingSettings(updated)` — a name that exists only in **AdminPanel's** scope (its service-layer import from `venueData.ts`), never destructured here. Every onChange threw `ReferenceError: setSpacingSettings is not defined` before the state setter, the success toast, or any persistence could run. The intended call — the `handleSaveSpacing` prop, which AdminPanel passes in commonProps and which does service-persist + state + toast — is now used at all 12 sites. The panel's controls now function.

Clone-stamp cleanup alongside: 57 invalid + 206 unused names; unused React import (jsx: react-jsx makes it safe); empty types import dropped.

## Pinned by

`SpacingManagement.typing.test.ts` (2 tests): all 12 sites call `handleSaveSpacing(updated)`; no `setSpacingSettings(` call remains; prop destructured; no global shadowing.

## Gates (batch)

tsc + strict unused-locals scan clean · eslint 0 errors / 30 warnings · vitest **1019 passed** / 5 skipped (+2) · single-file 550.32 kB gzip (still shrinking) + split chunks within budget · audit clean · ratchet 15 → **12**.

## Registry delta

Rows 1.8, 1.9, 1.10 → `done`. Phase 1: **12/24 — halfway**. Next: 1.11 `TemplateManagement.tsx` (770).
