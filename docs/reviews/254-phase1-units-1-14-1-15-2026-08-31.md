# Review #254 — Phase 1 Units 1.14–1.15: GuidelineManagement, AccessControlPanel

**Date:** 2026-08-31 · **Mode:** continuous campaign · Batch note: both units were pure dead-code removals (zero behavior change) and share one gate cycle; each has its own registry row.

## Unit 1.14 — `admin/GuidelineManagement.tsx` (697 → 429 lines)

Classic clone-stamp panel: 57 invalid destructure names (props that don't exist on `AdminCommonProps`) + 204 unused bindings removed; imports 9 lines → 3 (types import 16 → 1: only `Guideline` was used). **No defects** — none of the 57 invalid names were referenced in the body (verified by tsc after removal: zero "Cannot find name" errors), so no F-252-1-style runtime bug. Destructure went from ~260 entries to 7.

## Unit 1.15 — `admin/AccessControlPanel.tsx` (609 → 601 lines)

Original code (properly typed props, no clone-stamp destructure). On de-blinding, 2 tsc errors on `selectedRole.hierarchy` possibly undefined:

- **Not a defect** — `Role.hierarchy?: number` is optional; the badge comparison chain (`>= 90 / >= 40 / else`) falls through safely when undefined (renders "External Portal Role", no crash). Fixed with `(selectedRole.hierarchy ?? 0)` — identical runtime semantics, type-clean.

Dead code pruned: unwired `handleCopyPermissions` handler (its button was dropped in an earlier refactor — comment still said "Copy permissions + Delete buttons"; comment corrected), `copyPermissions` + `updateRole` from useRBAC destructure, `getChildPermissions` import, unused `React` import. Existing `AccessControlPanel.test.tsx` (2 tests) passes.

## Gates (batch)

tsc + strict unused-locals scan clean (non-test) · eslint 0 errors / 30 warnings · vitest **1021 passed** / 5 skipped · single-file 546.46 kB gzip + split chunks within budget · audit clean · ratchet 9 → **7** (script **and** baseline test updated together — see #253's CI note).

## Registry delta

Rows 1.14, 1.15 → `done`. Phase 1: **17/24**. Next: 1.16 `VenueChatPanel.tsx` (595).
