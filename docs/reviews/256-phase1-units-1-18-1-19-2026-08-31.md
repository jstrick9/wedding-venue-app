# Review #256 — Phase 1 Units 1.18–1.19: SecurityAuditManagement, CommunicationTemplatesManagement

**Date:** 2026-08-31 · **Mode:** continuous campaign · Batch note: both units are bug units; each pinned in `CommunicationTemplatesManagement.typing.test.ts` (shared file, 3 tests covering both components). One gate cycle.

## Unit 1.18 — `admin/SecurityAuditManagement.tsx` (345 → 344 lines)

**F-256-1 (P3 UX bug — fixed, same family as F-255-1): every Security & Audit action has saved silently since creation.** The panel destructured `onShowSuccess` from `AdminCommonProps`, but the prop is named `showSuccess` (AdminPanel passes `commonProps`). Always `undefined` → the 5 optional-chained toasts never fired: save security settings, clear expired sessions & cache (2 outcome variants), export audit log as CSV, export as JSON. Data still saved (P3 not P1). All 5 sites now call `showSuccess`.

The component's own test fixture had mocked the phantom name **and asserted against it** (lines 44/52) — the test codified the bug. Fixture upgraded to `showSuccess: vi.fn()` and assertions updated; both tests still pass and now assert the real prop.

## Unit 1.19 — `admin/CommunicationTemplatesManagement.tsx` (345 → 373 lines)

**F-256-1 (continued): same phantom-prop defect** — 5 silent-success sites (create template, remove template, reset defaults, save email wording, copy merge tag). Fixed the same way; fixture upgraded alongside. Net +28 lines because F-256-2's fix adds a loader/persist helper.

**F-256-2 (P3 UX bug — fixed): "Save Wording Defaults" was a complete no-op.** The Email Invite Wording tab's save button toasted "Email invite wording defaults saved!" while persisting **nothing**: `emailSubject`/`emailBody` were hardcoded-initial local state with no storage read or write anywhere (and no other consumer in the codebase). Every admin edit was silently discarded on reload — a fake save. Now: wording persists to `STORAGE_KEYS.EMAIL_WORDING_DEFAULTS` (new key, same pattern as the file's own `saveCommunicationTemplates`), state initializes via `loadEmailWording()` (with the previous hardcoded strings as fallback), and the save emits data-changed before toasting.

Dead code pruned alongside: unused `BrandedTips` import; `editingId`/`setEditingId` state (an edit-in-place flow that was never implemented — grep-verified only the declaration exists).

## Pinned by

`CommunicationTemplatesManagement.typing.test.ts` (3 tests): both components call the real `showSuccess` prop at all 5 sites each with zero `onShowSuccess` references; the wording save writes `STORAGE_KEYS.EMAIL_WORDING_DEFAULTS` + emits data-changed; editors initialize from `loadEmailWording()`.

## Gates (batch)

tsc + strict unused-locals scan clean (non-test) · eslint 0 errors / 30 warnings · vitest **1026 passed** / 5 skipped (+4) · single-file 546.50 kB gzip + split chunks within budget · audit clean · ratchet 5 → **3**.

## Registry delta

Rows 1.18, 1.19 → `done`. Phase 1: **21/24**. Next: 1.20 `CoupleLayoutEditor.tsx` (310).
